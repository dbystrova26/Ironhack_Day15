This lab builds a local filesystem MCP server, connects it to LangChain with `MultiServerMCPClient`, loads MCP tools and resources into an agent, and compares that workflow with a direct OpenAI API path using the same workspace data. It shows how the MCP layer standardizes access to tools and resources while still letting the project fall back to direct API calls for simpler single-integration use cases.
